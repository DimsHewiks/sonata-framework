<?php

namespace Sonata\Framework\Http;

abstract class ParamsDTO
{
    public function __construct(array $data)
    {
        foreach ($data as $key => $value) {
            if (property_exists($this, $key)) {
                $this->{$key} = $value;
            }
        }
    }
}